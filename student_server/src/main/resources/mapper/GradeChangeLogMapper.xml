<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auggie.student_server.mapper.GradeChangeLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO studentms.grade_change_log (
            score_id, operation, operator_id, operator_name, before_json, after_json
        ) VALUES (
            #{scoreId}, #{operation}, #{operatorId}, #{operatorName}, #{beforeJson}, #{afterJson}
        )
    </insert>

    <select id="findByScoreId" resultType="GradeChangeLog">
        SELECT id, score_id AS scoreId, operation, operator_id AS operatorId, operator_name AS operatorName,
               before_json AS beforeJson, after_json AS afterJson, created_at AS createdAt
        FROM studentms.grade_change_log
        WHERE score_id = #{scoreId}
        ORDER BY created_at DESC
    </select>
</mapper>
