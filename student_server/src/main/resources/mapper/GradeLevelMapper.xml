<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auggie.student_server.mapper.GradeLevelMapper">
    <select id="findAll" resultType="GradeLevel">
        SELECT id, name, sort_order AS sortOrder
        FROM studentms.grade_level
        ORDER BY sort_order ASC, id
    </select>

    <select id="findById" resultType="GradeLevel">
        SELECT id, name, sort_order AS sortOrder
        FROM studentms.grade_level
        WHERE id = #{id}
    </select>

    <select id="findByName" resultType="GradeLevel">
        SELECT id, name, sort_order AS sortOrder
        FROM studentms.grade_level
        WHERE name = #{name}
    </select>
</mapper>
