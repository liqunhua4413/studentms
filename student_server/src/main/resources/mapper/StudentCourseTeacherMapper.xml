<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auggie.student_server.mapper.StudentCourseTeacherMapper">
    <select id="findByStudentId" resultType="CourseTeacherInfo">
        SELECT c.id AS course_id, c.cname, t.id AS teacher_id, t.tname, sct.grade, c.ccredit
        FROM studentms.score sct INNER JOIN studentms.teacher t ON sct.teacher_id = t.id
                INNER JOIN studentms.course c ON sct.course_id = c.id
        WHERE sct.student_id = #{sid} AND sct.term = #{term}
    </select>

    <select id="findBySearch" resultType="SCTInfo">
        SELECT c.id AS course_id, c.cname, t.id AS teacher_id, t.tname, s.id AS student_id, s.sname, sct.grade, sct.term,
               sct.usual_grade, sct.final_grade, sct.total_grade, sct.class_id, sct.major_id, sct.department_id
        FROM studentms.score sct INNER JOIN studentms.student s ON sct.student_id = s.id
                INNER JOIN studentms.teacher t ON sct.teacher_id = t.id
                INNER JOIN studentms.course c ON sct.course_id = c.id
        <where>
            <if test="sid != null">
                s.id = #{sid}
            </if>
            <if test="tid != null">
                AND t.id = #{tid}
            </if>
            <if test="cid != null">
                AND c.id = #{cid}
            </if>
            <if test="sname != null">
                <if test="sFuzzy == 0">
                    AND s.sname = #{sname}
                </if>
                <if test="sFuzzy == 1">
                    AND s.sname LIKE CONCAT('%', #{sname}, '%')
                </if>
            </if>
            <if test="tname != null">
                <if test="tFuzzy == 0">
                    AND t.tname = #{tname}
                </if>
                <if test="tFuzzy == 1">
                    AND t.tname LIKE CONCAT('%', #{tname}, '%')
                </if>
            </if>
            <if test="cname != null">
                <if test="cFuzzy == 0">
                    AND c.cname = #{cname}
                </if>
                <if test="cFuzzy == 1">
                    AND c.cname LIKE CONCAT('%', #{cname}, '%')
                </if>
            </if>
            <if test="term != null">
                AND sct.term = #{term}
            </if>
            <if test="classId != null">
                AND sct.class_id = #{classId}
            </if>
            <if test="majorId != null">
                AND sct.major_id = #{majorId}
            </if>
            <if test="departmentId != null">
                AND sct.department_id = #{departmentId}
            </if>
            <if test="lowBound != null">
                AND (sct.grade >= #{lowBound} OR sct.total_grade >= #{lowBound})
            </if>
            <if test="highBound != null">
                AND (#{highBound} >= sct.grade OR #{highBound} >= sct.total_grade)
            </if>
        </where>
    </select>

    <insert id="batchInsert">
        INSERT INTO studentms.score (student_id, course_id, teacher_id, grade, term, usual_grade, final_grade, total_grade, class_id, major_id, department_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.studentId}, #{item.courseId}, #{item.teacherId}, #{item.grade}, #{item.term}, #{item.usualGrade}, #{item.finalGrade}, #{item.totalGrade}, #{item.classId}, #{item.majorId}, #{item.departmentId})
        </foreach>
    </insert>
</mapper>