<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auggie.student_server.mapper.ScoreImportRecordMapper">

    <insert id="save">
        INSERT INTO studentms.score_import_record
        (file_name, file_path, term, course_id, teacher_id, department_id, operator, status, message, created_at)
        VALUES
        (#{record.fileName}, #{record.filePath}, #{record.term}, #{record.courseId}, #{record.teacherId},
         #{record.departmentId}, #{record.operator}, #{record.status}, #{record.message}, #{record.createdAt})
    </insert>

    <select id="findAll" resultType="ScoreImportRecord">
        SELECT * FROM studentms.score_import_record ORDER BY created_at DESC
    </select>

    <select id="findByDepartmentId" resultType="ScoreImportRecord">
        SELECT * FROM studentms.score_import_record 
        WHERE department_id = #{departmentId}
        ORDER BY created_at DESC
    </select>

    <delete id="deleteById">
        DELETE FROM studentms.score_import_record WHERE id = #{id}
    </delete>

    <select id="findById" resultType="ScoreImportRecord">
        SELECT * FROM studentms.score_import_record WHERE id = #{id}
    </select>

</mapper>

